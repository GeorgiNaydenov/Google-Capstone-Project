Material Symbols Outlined glyph wrapper; the system's only icon primitive.

```jsx
<Icon name="dashboard" />
<Icon name="warning" size={16} fill={1} />
```

`name` is the ligature; `size` also sets optical sizing. Axes: `fill` (0/1), `weight`, `grade`.
