Icon-only square button for app bars and panel headers; subtle tonal hover, no shadow.

```jsx
<IconButton icon="settings" label="Settings" />
<IconButton icon="bar_chart" label="Bar chart" active />
```

Sizes `sm` (28px) / `md` (32px). Pass `active` for the selected/primary-tonal state.
