"""Local clinical data utilities used by the deterministic public demo.

The former ADK agent bootstrap is intentionally absent: importing this package
must never load or initialize a paid agent runtime.
"""
