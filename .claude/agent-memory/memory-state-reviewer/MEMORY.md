# Memory and State Reviewer Memory

- ADK state prefixes have distinct persistence scopes; `temp:` is invocation-only.
- Long-term memory and A2A context require recursive PHI, PII, and secret filtering.
- Deterministic memory contracts are pytest concerns; response quality is eval.
